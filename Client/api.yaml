/data:
  get:
    description: client使用自己密钥加密过后的信息，发送给computing center。
    params:
      filters:
        description: 过滤条件
      N: # HEX
      k: # HEX
      g: # HEX
    success:
      msg: ok
      A: # HEX
      B: # HEX
      pk: # HEX
    error:
      msg: <ERROR MESSAGE>
  post:
    description: computing center完成了计算，将结果发回client
    body:
      W:
      Z:
    success:
      msg: ok
    error:
      msg: <ERROR MESSAGE>
